// import { ICannonConfiguration } from "../../Configurations/IConfiguration";
// import { IArea } from "../../Models/Area/IArea";

export class CannonRender {
    // public static draw (canvas: IArea, cannon: ICannonConfiguration){
    //     canvas.context.save();
    //     canvas.context.translate(cannon.xStartingPosition, cannon.yStartingPosition);
    //     canvas.context.rotate(cannon.angleStarting * cannon.rotationFactor);
    //     canvas.context.drawImage(, -25, -25, this.width, this.height);
    //     canvas.context.restore();

    // }
}